import express from 'express'; import {protect} from '../middleware/authMiddleware.js'; import {adminOnly} from '../middleware/adminMiddleware.js'; import {submitContact,getMessages,markMessageRead,deleteMessage} from '../controllers/contactController.js'; const r=express.Router(); r.post('/',submitContact); r.get('/',protect,adminOnly,getMessages); r.put('/:id/read',protect,adminOnly,markMessageRead); r.delete('/:id',protect,adminOnly,deleteMessage); export default r;