import express from 'express'; import {protect} from '../middleware/authMiddleware.js'; import {adminOnly} from '../middleware/adminMiddleware.js'; import {getFaqs,createFaq,updateFaq,deleteFaq} from '../controllers/faqController.js'; const r=express.Router(); r.get('/',getFaqs); r.post('/',protect,adminOnly,createFaq); r.put('/:id',protect,adminOnly,updateFaq); r.delete('/:id',protect,adminOnly,deleteFaq); export default r;