import express from 'express'; import {protect} from '../middleware/authMiddleware.js'; import {adminOnly} from '../middleware/adminMiddleware.js'; import {getInformation,getArticle,createArticle,updateArticle,deleteArticle,publishArticle,toggleImportant,toggleBookmarkArticle} from '../controllers/informationController.js'; const r=express.Router(); r.get('/',getInformation); r.get('/:slug',getArticle); r.post('/',protect,adminOnly,createArticle); r.put('/:id',protect,adminOnly,updateArticle); r.delete('/:id',protect,adminOnly,deleteArticle); r.put('/:id/publish',protect,adminOnly,publishArticle); r.put('/:id/important',protect,adminOnly,toggleImportant); r.post('/:id/bookmark',protect,toggleBookmarkArticle); export default r;