import express from 'express'; import {protect} from '../middleware/authMiddleware.js'; import {adminOnly} from '../middleware/adminMiddleware.js'; import {getNotifications,createNotification,markRead,markAllRead} from '../controllers/notificationController.js'; const r=express.Router(); r.get('/',protect,getNotifications); r.post('/',protect,adminOnly,createNotification); r.put('/:id/read',protect,markRead); r.put('/read-all',protect,markAllRead); export default r;