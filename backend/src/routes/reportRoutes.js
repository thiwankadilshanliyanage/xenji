import express from 'express'; import {protect} from '../middleware/authMiddleware.js'; import {adminOnly} from '../middleware/adminMiddleware.js'; import {submitReport,getReports,updateReportStatus,deleteReport} from '../controllers/reportController.js'; const r=express.Router(); r.post('/',protect,submitReport); r.get('/',protect,adminOnly,getReports); r.put('/:id/status',protect,adminOnly,updateReportStatus); r.delete('/:id',protect,adminOnly,deleteReport); export default r;