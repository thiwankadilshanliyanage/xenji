import express from 'express'; import {protect} from '../middleware/authMiddleware.js'; import {adminOnly} from '../middleware/adminMiddleware.js'; import {getServices,getService,createService,updateService,deleteService,publishService,featureService,toggleBookmarkService} from '../controllers/serviceController.js'; const r=express.Router(); r.get('/',getServices); r.get('/:slug',getService); r.post('/',protect,adminOnly,createService); r.put('/:id',protect,adminOnly,updateService); r.delete('/:id',protect,adminOnly,deleteService); r.put('/:id/publish',protect,adminOnly,publishService); r.put('/:id/feature',protect,adminOnly,featureService); r.post('/:id/bookmark',protect,toggleBookmarkService); export default r;