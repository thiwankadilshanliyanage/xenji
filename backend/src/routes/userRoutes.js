import express from 'express'; import {protect} from '../middleware/authMiddleware.js'; import {uploadImage} from '../middleware/uploadMiddleware.js'; import {getProfile,updateProfile,updateAvatar,changePassword,deleteAccount,getUserBookmarks} from '../controllers/userController.js'; const r=express.Router(); r.use(protect); r.get('/profile',getProfile); r.put('/profile',updateProfile); r.put('/avatar',uploadImage.single('avatar'),updateAvatar); r.put('/change-password',changePassword); r.delete('/delete-account',deleteAccount); r.get('/bookmarks',getUserBookmarks); export default r;